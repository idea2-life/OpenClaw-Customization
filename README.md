# OpenClaw Customizations

Multiple scripts to improve OpenClaw.

## pretty-sign-in

Inline startup menu for OpenClaw VPS environments. Presents options on login to launch claude, openclaw tui, or type a custom command. See `pretty-sign-in/startup_menu.py` header for setup instructions.
